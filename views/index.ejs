<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>MY APP</title>
</head>
<body>
May Node and Express be with you.

<form action="/quotes" method="POST">
    <input type="text" placeholder="name" name="name">
    <input type="text" placeholder="quote" name="quote">
    <button type="submit">Submit</button>
</form>

<div>
    <label>
        Replace last quote written by Master Yoda with a quote written by Darth Vader:
        <button id="update"> Darth Vader invades! </button>
    </label>
</div>

<div>
    <label>
        Delete Darth Vadar's first quote:
        <button id="delete"> Delete first Darth Vader quote </button>
    </label>

</div>

<ul class="quotes">
    <% for(var i = quotes.length - 1; i >= 0; i--) { %>
        <li class="quote">
            <span><%= quotes[i].name %></span>
            <span><%= quotes[i].quote %></span>
        </li>
    <% } %>
</ul>
<script src="main.js"></script>
</body>
</html>